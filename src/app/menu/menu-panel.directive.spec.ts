import { MenuPanelDirective } from './menu-panel.directive';

describe('MenuPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuPanelDirective();
    expect(directive).toBeTruthy();
  });
});
